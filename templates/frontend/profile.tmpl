<div class="profile-container">
    <img src="{{.profile.Picture}}" class="img-thumbnail profile-image pull-left" />
    <div>
        <h1 class="profile-h1">{{.profile.Name}}</h1>
        <p>{{.profile.Email}}</p>
    </div>
</div>
